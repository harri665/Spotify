# Spotify sp_dc cookie - Required
# Get this from your browser when logged into Spotify
# Go to https://open.spotify.com/ → DevTools → Application → Cookies → sp_dc
SP_DC_COOKIE=your_sp_dc_cookie_here

# Check interval in milliseconds (default: 3 minutes = 180000ms)
CHECK_INTERVAL=180000

# Docker environment
NODE_ENV=production
