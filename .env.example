# Spotify Configuration
# Get your sp_dc cookie from browser when logged into Spotify
# Go to https://open.spotify.com/ → DevTools → Application → Cookies → sp_dc
SP_DC_COOKIE=your_spotify_sp_dc_cookie_here

# Monitoring Configuration
# Check interval in milliseconds (default: 30 seconds = 30000ms)
CHECK_INTERVAL=30000

# Dashboard Configuration
PORT=3000

# Environment
NODE_ENV=development
